import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textInput: {
    borderWidth: 2,
    borderRadius: 8,
    paddingHorizontal: 18,
    paddingVertical: 12,
    borderColor: "#27272a",
    color: "#e5e7eb"
  },
  focus: {
    borderColor: "#e5e7eb"
  }
});
